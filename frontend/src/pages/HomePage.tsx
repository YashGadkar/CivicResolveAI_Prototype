import { ArrowRight, Building2, ChevronRight, CircleDot, Gauge, Landmark, Search, Send, TicketCheck, Workflow } from "lucide-react";
import { Link } from "react-router-dom";
import { Button } from "../components/ui/button";
import { Card, CardContent } from "../components/ui/card";
import type { User } from "../types";

export function HomePage({user}:{user:User}){const staff=["OFFICER","ADMIN"].includes(user.role);const cards=staff?[[Building2,"Ticket operations","Review civic complaints and current workflow status.","/officer"],[Gauge,"Progress visibility","See which tickets are new, active, escalated or resolved.","/officer"],[Workflow,"AI Centre","Test complaint understanding in a focused workspace.","/command-center"]]:[[Send,"Submit a complaint","Describe the issue naturally and add a locality when available.","/submit"],[TicketCheck,"My complaints","See tickets created from your account and their latest state.","/mine"],[Search,"Track a ticket","Open a ticket directly with its CivicResolve reference number.","/track"]];return <main><section className="hero-mesh border-b border-slate-200"><div className="mx-auto max-w-7xl px-4 py-16 lg:px-8 lg:py-24"><div className="max-w-4xl"><div className="mb-5 inline-flex items-center rounded-full border border-teal-100 bg-white px-3 py-1.5 text-xs font-bold text-teal-800 shadow-sm"><CircleDot size={13} className="mr-2"/>{staff?"Employee operations workspace":"Citizen complaint services"}</div><h1 className="text-4xl font-black tracking-[-.04em] text-[#081525] sm:text-6xl">{staff?"Move every complaint toward resolution.":"Report civic issues clearly. Track action transparently."}</h1><p className="mt-6 max-w-3xl text-lg leading-8 text-slate-600">{staff?"Review routed tickets, see their current progress and keep the citizen-facing record moving.":"Describe the problem in your own language. CivicResolve identifies the civic issue and helps turn it into a trackable ticket."}</p><div className="mt-8 flex flex-wrap gap-3">{staff?<Link to="/officer"><Button size="lg">Open ticket operations <ArrowRight className="ml-2" size={18}/></Button></Link>:<><Link to="/submit"><Button size="lg">Submit complaint <ArrowRight className="ml-2" size={18}/></Button></Link><Link to="/track"><Button size="lg" variant="outline">Track ticket</Button></Link></>}</div></div></div></section><section className="mx-auto max-w-7xl px-4 py-10 lg:px-8"><div className="grid gap-4 md:grid-cols-3">{cards.map(([Icon,title,body,path])=>{const I=Icon as typeof Landmark;return <Link to={String(path)} key={String(title)}><Card className="group h-full border-slate-200 transition hover:-translate-y-1 hover:border-teal-200 hover:shadow-panel"><CardContent className="pt-6"><span className="grid h-11 w-11 place-items-center rounded-xl bg-[#0b1f3a] text-white"><I size={20}/></span><h3 className="mt-5 text-lg font-black text-slate-900">{String(title)}</h3><p className="mt-2 text-sm leading-6 text-slate-500">{String(body)}</p><span className="mt-5 inline-flex items-center text-sm font-bold text-teal-700">Open <ChevronRight size={16}/></span></CardContent></Card></Link>})}</div></section></main>}
