import { useEffect, useMemo, useState } from "react";
import type { ReactNode } from "react";
import { AlertTriangle, Building2, FileUp, Mail, MapPin, Phone, Search, ShieldAlert, UserRound, Users, X } from "lucide-react";
import { api } from "../api";
import { Kpi, NoticeState, Page, ProgressBar, Toast } from "../components/app-ui";
import { Badge } from "../components/ui/badge";
import { Button } from "../components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "../components/ui/card";
import type { EnrichedTicket } from "../types";

export function OfficerPage(){
  const[tickets,setTickets]=useState<EnrichedTicket[]>([]);const[filter,setFilter]=useState("ALL");const[search,setSearch]=useState("");const[notice,setNotice]=useState<NoticeState>(null);const[selected,setSelected]=useState<EnrichedTicket|null>(null);const[busy,setBusy]=useState(false);
  async function refresh(){try{const data=await api.platformStaffTickets();setTickets(data);if(selected){const latest=data.find(x=>x.ticket.ticket_code===selected.ticket.ticket_code);if(latest)setSelected(latest)}}catch(e){setNotice({tone:"error",title:"Could not load ticket queue",message:e instanceof Error?e.message:"Please retry."})}}
  useEffect(()=>{void refresh()},[]);// eslint-disable-line react-hooks/exhaustive-deps
  const shown=useMemo(()=>tickets.filter(item=>{const t=item.ticket;const ok=filter==="ALL"||t.status===filter;const hay=`${t.ticket_code} ${t.category} ${t.location} ${t.department} ${item.citizen?.name||""} ${t.complaint}`.toLowerCase();return ok&&hay.includes(search.trim().toLowerCase())}),[tickets,filter,search]);
  const kpis=[["Total",tickets.length],["New",tickets.filter(x=>x.ticket.status==="SUBMITTED").length],["Active",tickets.filter(x=>["ASSIGNED","IN_PROGRESS"].includes(x.ticket.status)).length],["Escalated",tickets.filter(x=>x.ticket.status==="ESCALATED").length],["Safety flags",tickets.filter(x=>x.meta.emergency).length]];
  async function quickStatus(item:EnrichedTicket,status:string){setBusy(true);try{await api.updateStatus(item.ticket.ticket_code,status,`Employee updated ticket to ${status}.`);await refresh();setNotice({tone:"success",title:"Ticket updated",message:`${item.ticket.ticket_code} is now ${status.replaceAll("_"," ").toLowerCase()}.`})}catch(e){setNotice({tone:"error",title:"Status update failed",message:e instanceof Error?e.message:"Please retry."})}finally{setBusy(false)}}
  return <Page title="Civic operations desk" eyebrow="Employee workspace" subtitle="Review the original citizen statement, service context, related incidents, evidence and accountability trail before taking action.">{notice&&<Toast notice={notice} onClose={()=>setNotice(null)}/>} {selected&&<TicketDrawer item={selected} onClose={()=>setSelected(null)} onChanged={async()=>{await refresh();setSelected(await api.platformTicket(selected.ticket.ticket_code))}} notify={setNotice}/>}<div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-5">{kpis.map(([l,v])=><Kpi key={String(l)} label={String(l)} value={String(v)}/>)}</div><Card className="mt-6 overflow-hidden border-slate-200 shadow-panel dark:border-slate-800 dark:bg-slate-900"><div className="flex flex-col gap-3 border-b border-slate-200 bg-white p-4 dark:border-slate-800 dark:bg-slate-900 lg:flex-row lg:items-center lg:justify-between"><div className="relative max-w-xl flex-1"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" size={16}/><input className="focus-field pl-9" placeholder="Search ticket, citizen, complaint, category or location…" value={search} onChange={e=>setSearch(e.target.value)}/></div><select className="rounded-xl border border-slate-300 bg-white px-3 py-2.5 text-sm font-semibold dark:border-slate-700 dark:bg-slate-800 dark:text-white" value={filter} onChange={e=>setFilter(e.target.value)}>{["ALL","SUBMITTED","ASSIGNED","IN_PROGRESS","ESCALATED","RESOLVED"].map(s=><option key={s}>{s}</option>)}</select></div><div className="overflow-x-auto"><table className="w-full min-w-[1240px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500 dark:bg-slate-800 dark:text-slate-300"><tr>{["Ticket","Citizen","Issue","Location","Department","Priority","Progress","Incident","Actions"].map(h=><th key={h} className="px-4 py-3">{h}</th>)}</tr></thead><tbody>{shown.map(item=>{const t=item.ticket;return <tr className="border-t border-slate-100 bg-white transition hover:bg-slate-50/70 dark:border-slate-800 dark:bg-slate-900 dark:hover:bg-slate-800/60" key={t.ticket_code}><td className="px-4 py-4"><div className="font-black text-slate-900 dark:text-white">{t.ticket_code}</div><div className="mt-1 text-xs text-slate-400">{t.language}</div>{item.meta.emergency&&<div className="mt-2 inline-flex items-center gap-1 rounded-full bg-rose-50 px-2 py-1 text-[10px] font-black uppercase text-rose-700 dark:bg-rose-950/40 dark:text-rose-200"><ShieldAlert size={11}/>Safety</div>}</td><td className="px-4 py-4"><b className="text-slate-800 dark:text-slate-100">{item.citizen?.name||"Citizen"}</b><div className="mt-1 max-w-44 truncate text-xs text-slate-400">{item.citizen?.email||"No account detail"}</div></td><td className="px-4 py-4"><div className="font-semibold">{t.category}</div><div className="mt-1 max-w-56 line-clamp-2 text-xs leading-5 text-slate-500 dark:text-slate-400">{t.complaint}</div></td><td className="px-4 py-4"><div className="max-w-48 line-clamp-2">{t.location}</div>{item.meta.ward&&<div className="mt-1 text-xs text-slate-400">{item.meta.ward}</div>}</td><td className="px-4 py-4">{t.department}</td><td className="px-4 py-4"><Badge value={t.priority}/></td><td className="px-4 py-4"><ProgressBar ticket={t} compact/></td><td className="px-4 py-4">{item.meta.related_reports>1?<div className="flex items-center gap-1 font-bold text-amber-700 dark:text-amber-300"><Users size={15}/>{item.meta.related_reports} reports</div>:<span className="text-xs text-slate-400">Single report</span>}</td><td className="px-4 py-4"><div className="flex flex-wrap gap-2"><Button size="sm" variant="outline" onClick={()=>setSelected(item)}>Open</Button>{t.status==="SUBMITTED"&&<Button size="sm" onClick={()=>void quickStatus(item,"ASSIGNED")} disabled={busy}>Accept</Button>}{["ASSIGNED","ESCALATED"].includes(t.status)&&<Button size="sm" onClick={()=>void quickStatus(item,"IN_PROGRESS")} disabled={busy}>Start work</Button>}</div></td></tr>})}</tbody></table>{shown.length===0&&<div className="p-10 text-center text-sm text-slate-500">No tickets match this view.</div>}</div></Card></Page>
}

function TicketDrawer({item,onClose,onChanged,notify}:{item:EnrichedTicket;onClose:()=>void;onChanged:()=>Promise<void>;notify:(n:NoticeState)=>void}){
  const t=item.ticket;const[officer,setOfficer]=useState(t.assigned_officer||"");const[department,setDepartment]=useState(t.department);const[reason,setReason]=useState("");const[note,setNote]=useState(item.meta.resolution_note||"");const[file,setFile]=useState<File|null>(null);const[busy,setBusy]=useState(false);
  async function act(fn:()=>Promise<unknown>,success:string){setBusy(true);try{await fn();await onChanged();notify({tone:"success",title:"Ticket updated",message:success})}catch(e){notify({tone:"error",title:"Action failed",message:e instanceof Error?e.message:"Please retry."})}finally{setBusy(false)}}
  async function upload(){if(!file)return;await act(()=>api.uploadEvidence(t.ticket_code,file,"RESOLUTION_EVIDENCE"),"Resolution evidence attached to the ticket.");setFile(null)}
  return <div className="fixed inset-0 z-50 bg-slate-950/55 backdrop-blur-sm"><div className="absolute inset-y-0 right-0 w-full max-w-3xl overflow-y-auto border-l border-slate-200 bg-white shadow-2xl dark:border-slate-800 dark:bg-[#0b1726]"><div className="sticky top-0 z-10 flex items-start justify-between border-b border-slate-200 bg-white/95 p-5 backdrop-blur dark:border-slate-800 dark:bg-[#0b1726]/95"><div><p className="text-xs font-black uppercase tracking-[.18em] text-teal-700 dark:text-teal-300">Employee ticket detail</p><h2 className="mt-1 text-2xl font-black text-slate-950 dark:text-white">{t.ticket_code}</h2></div><button className="rounded-xl border border-slate-200 p-2 text-slate-500 hover:bg-slate-50 dark:border-slate-700 dark:hover:bg-slate-800" onClick={onClose}><X/></button></div><div className="space-y-5 p-5"><section className="rounded-3xl bg-[linear-gradient(135deg,#0b1f3a,#0f766e)] p-6 text-white"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="text-xs font-black uppercase tracking-[.18em] text-teal-100">{t.category}</div><h3 className="mt-2 text-2xl font-black">Original citizen statement</h3><p className="mt-3 max-w-2xl text-sm leading-7 text-slate-100">{t.complaint}</p></div><Badge value={t.priority}/></div></section><div className="grid gap-4 md:grid-cols-2"><Info icon={<UserRound/>} title="Citizen"><b>{item.citizen?.name||"Unknown citizen"}</b><p>{item.citizen?.email||"No email available"}</p>{item.citizen?.contact&&<p><Phone className="mr-1 inline" size={13}/>{item.citizen.contact}</p>}</Info><Info icon={<MapPin/>} title="Location"><b>{t.location}</b><p>{[item.meta.ward,item.meta.zone,item.meta.city].filter(Boolean).join(" · ")||"No ward/zone metadata"}</p></Info></div>{item.meta.emergency&&<div className="flex gap-3 rounded-2xl border border-rose-200 bg-rose-50 p-4 text-sm text-rose-900 dark:border-rose-900 dark:bg-rose-950/30 dark:text-rose-100"><AlertTriangle className="shrink-0"/><div><b>Potential immediate safety risk</b><p className="mt-1 text-xs leading-5 opacity-80">Prioritize human review. The platform does not replace emergency dispatch.</p></div></div>}{item.meta.related_reports>1&&<div className="rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900 dark:border-amber-900 dark:bg-amber-950/30 dark:text-amber-200"><b>{item.meta.related_reports} related citizen reports</b><p className="mt-1 text-xs leading-5">This ticket belongs to a broader incident cluster for the same category and location.</p></div>}<Card className="dark:border-slate-800 dark:bg-slate-900"><CardHeader><CardTitle>Ownership & routing</CardTitle></CardHeader><CardContent className="space-y-4"><div className="grid gap-3 sm:grid-cols-[1fr_auto]"><input className="focus-field" value={officer} onChange={e=>setOfficer(e.target.value)} placeholder="Officer/team name"/><Button onClick={()=>void act(()=>api.assignTicket(t.ticket_code,officer),officer?`Assigned to ${officer}.`:"Ticket unassigned.")} disabled={busy}>Assign</Button></div><div className="grid gap-3"><input className="focus-field" value={department} onChange={e=>setDepartment(e.target.value)} placeholder="Department"/><textarea className="focus-field min-h-20" value={reason} onChange={e=>setReason(e.target.value)} placeholder="Reason for department transfer"/><Button variant="outline" onClick={()=>void act(()=>api.transferTicket(t.ticket_code,department,reason),`Transferred to ${department}.`)} disabled={busy||department.trim().length<2||reason.trim().length<3}><Building2 className="mr-2" size={16}/>Transfer department</Button></div></CardContent></Card><Card className="dark:border-slate-800 dark:bg-slate-900"><CardHeader><CardTitle>Resolution evidence</CardTitle></CardHeader><CardContent><div className="space-y-2">{item.attachments.map(a=><a href={api.evidenceUrl(a.id)} target="_blank" rel="noreferrer" className="flex items-center justify-between rounded-xl border border-slate-200 p-3 text-sm dark:border-slate-700" key={a.id}><span><FileUp className="mr-2 inline text-teal-700" size={15}/>{a.original_name}</span><span className="text-xs text-slate-400">{a.kind.replaceAll("_"," ")}</span></a>)}</div><div className="mt-4 flex gap-2"><input type="file" className="min-w-0 flex-1 text-xs" accept="image/jpeg,image/png,image/webp,video/mp4,application/pdf" onChange={e=>setFile(e.target.files?.[0]||null)}/><Button size="sm" variant="outline" onClick={()=>void upload()} disabled={!file||busy}>Upload</Button></div><textarea className="focus-field mt-4 min-h-28" value={note} onChange={e=>setNote(e.target.value)} placeholder="Describe what was done, what was inspected, and any follow-up required…"/><Button className="mt-3 w-full" onClick={()=>void act(()=>api.resolveWithNote(t.ticket_code,note),"Ticket marked resolved and sent for citizen confirmation.")} disabled={busy||note.trim().length<3}>Mark resolved with note</Button></CardContent></Card><Card className="dark:border-slate-800 dark:bg-slate-900"><CardHeader><CardTitle>Activity history</CardTitle></CardHeader><CardContent className="space-y-4">{t.audit_events.map((e,i)=><div className="border-l-2 border-teal-200 pl-4 dark:border-teal-800" key={`${e.event}-${i}`}><b className="text-sm text-slate-900 dark:text-white">{e.event.replaceAll("_"," ")}</b><p className="mt-1 text-sm leading-6 text-slate-500 dark:text-slate-400">{e.detail}</p><small className="text-slate-400">{new Date(e.created_at).toLocaleString()}</small></div>)}</CardContent></Card></div></div></div>
}

function Info({icon,title,children}:{icon:ReactNode;title:string;children:ReactNode}){return <div className="rounded-2xl border border-slate-200 bg-white p-4 dark:border-slate-800 dark:bg-slate-900"><div className="mb-3 flex items-center gap-2 text-xs font-black uppercase tracking-[.16em] text-teal-700 dark:text-teal-300"><span className="[&>svg]:h-4 [&>svg]:w-4">{icon}</span>{title}</div><div className="space-y-1 text-sm text-slate-600 dark:text-slate-300">{children}</div></div>}
