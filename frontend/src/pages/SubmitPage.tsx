import { FormEvent, useRef, useState } from "react";
import { Activity, CheckCircle2, FileUp, MapPinCheck, Mic, MicOff, Paperclip, ShieldAlert, Sparkles, TicketCheck, X } from "lucide-react";
import { api, type ComplaintPayload } from "../api";
import { Field, Metric, NoticeState, TicketView, Toast } from "../components/app-ui";
import { Button } from "../components/ui/button";
import { Card, CardContent } from "../components/ui/card";
import { Badge } from "../components/ui/badge";
import type { ComplaintAnalysis, ComplaintBatchAnalysis, Ticket } from "../types";

export function SubmitPage(){
  const[complaint,setComplaint]=useState("");const[location,setLocation]=useState("");const[landmark,setLandmark]=useState("");const[contact,setContact]=useState("");
  const[analysis,setAnalysis]=useState<ComplaintBatchAnalysis|null>(null);const[tickets,setTickets]=useState<Ticket[]>([]);const[busy,setBusy]=useState(false);const[notice,setNotice]=useState<NoticeState>(null);const[clarify,setClarify]=useState(false);const[listening,setListening]=useState(false);const[evidence,setEvidence]=useState<File[]>([]);const recognitionRef=useRef<any>(null);
  const payload:ComplaintPayload={complaint,location:location||undefined,landmark:landmark||undefined,contact:contact||undefined,verify_location:true};

  function toggleVoice(){
    if(listening){recognitionRef.current?.stop?.();setListening(false);return}
    const Ctor=(window as any).SpeechRecognition||(window as any).webkitSpeechRecognition;
    if(!Ctor){setNotice({tone:"info",title:"Voice input unavailable",message:"This browser does not expose speech recognition. You can continue typing normally."});return}
    const recognition=new Ctor();recognition.lang="en-IN";recognition.interimResults=false;recognition.continuous=true;
    recognition.onresult=(event:any)=>{let text="";for(let i=event.resultIndex;i<event.results.length;i++)text+=event.results[i][0].transcript+" ";setComplaint(v=>(v+" "+text).trim())};
    recognition.onerror=()=>{setListening(false);setNotice({tone:"error",title:"Voice input stopped",message:"Speech recognition could not continue. Your typed text is unchanged."})};
    recognition.onend=()=>setListening(false);recognitionRef.current=recognition;recognition.start();setListening(true)
  }

  async function analyze(e?:FormEvent){
    e?.preventDefault();setBusy(true);setNotice(null);
    try{
      const result=await api.analyzeBatch(payload);setAnalysis(result);setTickets([]);
      const needs=result.issues.some(issue=>issue.missing_information.length>0);
      if(needs)setClarify(true);else setNotice({tone:"success",title:result.issue_count>1?`${result.issue_count} civic issues identified`:"Complaint understood",message:result.issue_count>1?"Each issue will receive its own ticket.":"Review the routing and create your ticket."});
    }catch(err){setNotice({tone:"error",title:"Could not analyze complaint",message:err instanceof Error?err.message:"Please try again."})}finally{setBusy(false)}
  }

  async function create(){
    if(!analysis)return;setBusy(true);setNotice(null);
    try{
      const payloads=analysis.issues.map(issue=>({complaint:issue.source_text||complaint,location:issue.location||location||undefined,landmark:landmark||undefined,contact:contact||undefined,verify_location:true}));
      const created=await api.createTickets(payloads);setTickets(created);setAnalysis(null);
      if(evidence.length){for(const ticket of created){for(const file of evidence)await api.uploadEvidence(ticket.ticket_code,file,"CITIZEN_EVIDENCE")}}
      setNotice({tone:"success",title:created.length>1?`${created.length} tickets created`:"Ticket created",message:evidence.length?"Tickets created and supporting evidence uploaded successfully.":created.length>1?"Each detected civic problem now has a separate trackable ticket.":`${created[0].ticket_code} is ready to track.`});
    }catch(err){setNotice({tone:"error",title:"Ticket creation stopped",message:err instanceof Error?err.message:"Please retry."})}finally{setBusy(false)}
  }

  const ready=analysis?.issues.every(issue=>issue.missing_information.length===0)??false;
  const critical=analysis?.issues.some(issue=>issue.priority==="CRITICAL")??false;
  return <main className="mx-auto max-w-7xl px-4 py-10 lg:px-8"><div className="mb-8 flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-xs font-black uppercase tracking-[.22em] text-teal-700 dark:text-teal-300">Citizen service desk</p><h1 className="mt-2 text-3xl font-black tracking-tight text-[#081525] dark:text-white sm:text-4xl">Report a civic issue</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-500 dark:text-slate-400">Write or speak naturally. If your message contains several different civic problems, CivicResolve separates them and prepares one ticket for each.</p></div><div className="rounded-2xl border border-slate-200 bg-white px-4 py-3 text-xs text-slate-500 shadow-sm dark:border-slate-700 dark:bg-slate-900 dark:text-slate-400"><b className="text-slate-800 dark:text-slate-200">Privacy note:</b> add only details needed to resolve the civic issue.</div></div>{notice&&<Toast notice={notice} onClose={()=>setNotice(null)}/>} {clarify&&analysis&&<Clarification analysis={analysis} location={location} setLocation={setLocation} busy={busy} onClose={()=>setClarify(false)} onContinue={async()=>{setClarify(false);await analyze()}}/>}
  <div className="grid gap-6 lg:grid-cols-[.92fr_1.08fr]"><Card className="border-slate-200 shadow-panel dark:border-slate-700 dark:bg-slate-900"><CardContent className="pt-6"><form className="space-y-5" onSubmit={analyze}><Field label="Describe what happened"><div className="relative"><textarea className="focus-field min-h-60 resize-y pr-14" value={complaint} onChange={e=>setComplaint(e.target.value)} placeholder="Example: There is no water supply for three days. Also, garbage has not been collected near the market." maxLength={5000}/><button type="button" onClick={toggleVoice} className={`absolute bottom-3 right-3 grid h-10 w-10 place-items-center rounded-xl border transition ${listening?"border-rose-300 bg-rose-50 text-rose-600 dark:bg-rose-950":"border-slate-200 bg-white text-slate-500 hover:text-teal-700 dark:border-slate-700 dark:bg-slate-800 dark:text-slate-300"}`} aria-label={listening?"Stop voice input":"Start voice input"}>{listening?<MicOff size={18}/>:<Mic size={18}/>}</button></div>{listening&&<div className="mt-2 flex items-center gap-2 text-xs font-semibold text-rose-600 dark:text-rose-300"><span className="h-2 w-2 animate-pulse rounded-full bg-rose-500"/>Listening… speak naturally, then press the microphone again.</div>}</Field><div className="grid gap-4 sm:grid-cols-2"><Field label="Affected location"><input className="focus-field" value={location} onChange={e=>setLocation(e.target.value)} placeholder="Locality, ward, city or address"/></Field><Field label="Landmark (optional)"><input className="focus-field" value={landmark} onChange={e=>setLandmark(e.target.value)} placeholder="Nearby landmark"/></Field></div><Field label="Contact detail (optional)"><input className="focus-field" value={contact} onChange={e=>setContact(e.target.value)} placeholder="Phone or preferred contact reference" maxLength={255}/></Field><div className="rounded-2xl border border-dashed border-slate-300 bg-slate-50/70 p-4 dark:border-slate-700 dark:bg-slate-950/40"><div className="flex items-start gap-3"><Paperclip className="mt-0.5 text-teal-700 dark:text-teal-300" size={19}/><div className="flex-1"><b className="text-sm text-slate-900 dark:text-white">Supporting evidence</b><p className="mt-1 text-xs leading-5 text-slate-500 dark:text-slate-400">Optional JPG, PNG, WebP, MP4 or PDF. Files are uploaded only after a ticket is created.</p><input className="mt-3 block w-full text-xs text-slate-500 file:mr-3 file:rounded-lg file:border-0 file:bg-[#0b1f3a] file:px-3 file:py-2 file:font-bold file:text-white dark:text-slate-400" type="file" multiple accept="image/jpeg,image/png,image/webp,video/mp4,application/pdf" onChange={e=>setEvidence(Array.from(e.target.files||[]))}/>{evidence.length>0&&<div className="mt-2 text-xs font-semibold text-teal-700 dark:text-teal-300"><FileUp className="mr-1 inline" size={14}/>{evidence.length} file{evidence.length>1?"s":""} ready</div>}</div></div></div><Button size="lg" className="w-full" disabled={busy||complaint.trim().length<10}>{busy?<Activity className="mr-2 animate-spin" size={18}/>:<Sparkles className="mr-2" size={18}/>}Review complaint</Button></form></CardContent></Card><div>{critical&&<div className="mb-4 flex gap-3 rounded-2xl border border-rose-200 bg-rose-50 p-4 text-sm text-rose-900 dark:border-rose-900 dark:bg-rose-950/40 dark:text-rose-100"><ShieldAlert className="shrink-0" size={20}/><div><b>Potential immediate safety risk detected.</b><p className="mt-1 text-xs leading-5 opacity-80">CivicResolve is not an emergency dispatch service. If there is immediate danger to life or property, contact the appropriate emergency service while also submitting the civic report.</p></div></div>}{!analysis&&!tickets.length&&<Placeholder/>}{analysis&&<BatchSummary analysis={analysis} onCreate={create} busy={busy} ready={ready}/>} {tickets.length>0&&<div className="space-y-4">{tickets.map(ticket=><TicketView key={ticket.ticket_code} ticket={ticket}/>)}</div>}</div></div></main>
}

function BatchSummary({analysis,onCreate,busy,ready}:{analysis:ComplaintBatchAnalysis;onCreate:()=>void;busy:boolean;ready:boolean}){return <div className="space-y-4"><div className="rounded-2xl border border-teal-200 bg-teal-50 px-4 py-3 text-sm text-teal-900 dark:border-teal-900 dark:bg-teal-950/50 dark:text-teal-100"><div className="flex items-center gap-2 font-bold"><CheckCircle2 size={17}/>{analysis.issue_count>1?`${analysis.issue_count} separate problems detected`:`1 civic problem detected`}</div>{analysis.issue_count>1&&<p className="mt-1 text-xs opacity-80">Each problem will become an independent ticket and can be routed to a different department.</p>}</div>{analysis.issues.map((issue,index)=><IssueSummary key={`${issue.category}-${index}`} issue={issue} index={index}/>) }{ready&&<Button size="lg" className="w-full" onClick={onCreate} disabled={busy}><TicketCheck className="mr-2" size={18}/>{analysis.issue_count>1?`Create ${analysis.issue_count} trackable tickets`:"Create trackable ticket"}</Button>}</div>}

function IssueSummary({issue,index}:{issue:ComplaintAnalysis;index:number}){const ready=issue.missing_information.length===0;return <Card className="overflow-hidden border-slate-200 shadow-panel dark:border-slate-700 dark:bg-slate-900"><div className="border-b border-slate-200 bg-[#0b1f3a] px-6 py-4 text-white dark:border-slate-700"><div className="flex items-center justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-teal-200">Issue {index+1}</p><h2 className="mt-1 text-xl font-black">{issue.category}</h2></div><Badge value={issue.priority}/></div></div><CardContent className="pt-5"><div className="grid gap-3 sm:grid-cols-2"><Metric label="Detected language" value={issue.language}/><Metric label="Responsible department" value={issue.department}/><Metric label="Location" value={issue.location||"Needs verified location"}/><Metric label="Duration" value={issue.duration||"Not stated"}/><Metric label="Priority" value={issue.priority}/><Metric label="Readiness" value={ready?"Ready for ticket":"Needs clarification"}/></div>{issue.location_verified&&<div className="mt-4 flex items-start gap-2 rounded-xl border border-emerald-200 bg-emerald-50 p-3 text-xs text-emerald-800 dark:border-emerald-900 dark:bg-emerald-950/40 dark:text-emerald-200"><MapPinCheck size={16} className="mt-0.5 shrink-0"/><span><b>Verified place.</b> {issue.location_display_name||issue.location}</span></div>}{issue.duplicate_candidates.length>0&&<div className="mt-4 rounded-xl border border-amber-200 bg-amber-50 p-3 text-xs text-amber-900 dark:border-amber-900 dark:bg-amber-950/30 dark:text-amber-200"><b>Related reports found:</b> {issue.duplicate_candidates.map(d=>d.ticket_code).join(", ")}. These remain separate citizen reports but can be grouped operationally.</div>}<p className="mt-4 rounded-xl border border-slate-200 bg-slate-50 p-4 text-sm leading-6 text-slate-600 dark:border-slate-700 dark:bg-slate-800 dark:text-slate-300">{issue.citizen_response}</p></CardContent></Card>}

function Clarification({analysis,location,setLocation,busy,onClose,onContinue}:{analysis:ComplaintBatchAnalysis;location:string;setLocation:(v:string)=>void;busy:boolean;onClose:()=>void;onContinue:()=>void}){const issue=analysis.issues.find(item=>item.missing_information.length>0);const asks=issue?.missing_information.includes("location")??false;const message=issue?.location_verification_message||issue?.clarification_questions[0]||"Please add the missing information.";return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/45 p-4 backdrop-blur-sm"><div className="w-full max-w-md rounded-3xl border border-slate-200 bg-white p-6 shadow-2xl dark:border-slate-700 dark:bg-slate-900"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-amber-700 dark:text-amber-300">One more detail</p><h3 className="mt-1 text-lg font-black text-slate-900 dark:text-white">Help us route this correctly</h3></div><button className="rounded-lg p-1.5 text-slate-400 hover:bg-slate-100 dark:hover:bg-slate-800" onClick={onClose} aria-label="Close"><X size={17}/></button></div><p className="mt-3 text-sm leading-6 text-slate-600 dark:text-slate-300">{message}</p>{asks&&<div className="mt-4"><input className="focus-field" value={location} onChange={e=>setLocation(e.target.value)} placeholder="Locality, city, ward or address" autoFocus/><Button className="mt-3 w-full" onClick={onContinue} disabled={!location.trim()||busy}>{busy?"Verifying…":"Verify and continue"}</Button></div>}{!asks&&<Button className="mt-4 w-full" variant="outline" onClick={onClose}>Edit complaint</Button>}</div></div>}
function Placeholder(){return <Card className="border-dashed border-slate-300 bg-white/60 dark:border-slate-700 dark:bg-slate-900/60"><CardContent className="grid min-h-[500px] place-items-center text-center"><div className="max-w-md"><span className="mx-auto grid h-16 w-16 place-items-center rounded-2xl bg-teal-50 text-teal-700 dark:bg-teal-950 dark:text-teal-300"><Sparkles size={30}/></span><h3 className="mt-5 text-xl font-black text-slate-900 dark:text-white">A clean review appears here before anything is submitted</h3><p className="mt-2 text-sm leading-6 text-slate-500 dark:text-slate-400">CivicResolve separates different problems, checks the place, highlights possible related reports and shows the responsible department before creating tickets.</p></div></CardContent></Card>}
